// @ts-ignore
import APP from './../config/app.json';

/* istanbul ignore next */
export const { CONSTANTS: { UNAUTHORIZED = 'Acesso não autorizado!' } = {} } = APP;
/* istanbul ignore next */
export const { CONSTANTS: { FORBIDDEN = 'Acesso proibido' } = {} } = APP;
/* istanbul ignore next */
export const { CONSTANTS: { NOT_ALLOWED = 'Acesso não permitido' } = {} } = APP;
